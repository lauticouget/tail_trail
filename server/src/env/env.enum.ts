export enum EnvVarKeys {
  PORT = 'PORT',
  MONGO_URI = 'MONGO_URI',
  JWT_SECRET_KEY = 'JWT_SECRET_KEY',
  LOGS_LVL = 'LOGS_LVL',
  ENV_NAME = 'ENV_NAME',
}

export enum Environments {
  PROD = 'prod',
  DEV = 'dev',
  LOCAL = 'local',
}
